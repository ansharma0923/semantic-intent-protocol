# Default location for SIP persistent capability storage.
#
# The file capabilities.json will be created here at runtime when using
# JsonFileCapabilityStore (the default path is data/capabilities.json).
#
# You can override the path via the SIP_CAPABILITIES_FILE environment variable.
#
# This directory is tracked in git; the JSON file itself is excluded via .gitignore.
